export default defineEventHandler(async (event) => {

    //here we could do an API call with a private key, as the server zone will never be exposed to the user
    //to call this page, we just do:
    //const {data} = await useFetch('api/private')

    return (event)
})